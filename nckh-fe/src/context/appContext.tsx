import { io } from "socket.io-client";
import React from "react";
import { serverConfig } from "../const/serverConfig";
// export let socket = io(serverConfig.server);
// export const AppContext = React.createContext({socket})
